[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"

[project]
name = "architecture-agent"
version = "0.1.0"
description = "VibeOS Architecture Agent – designs systems, schemas, and APIs"
requires-python = ">=3.12"
dependencies = [
    "vibeos-agent",
    "fastapi",
    "uvicorn[standard]",
]
