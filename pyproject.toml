[project]
name = "vibeos"
version = "0.1.0"
description = "VibeOS – AI-native SDLC platform"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "agents/shared",
    "agents/*-agent",
    "platform/*",
]
